{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "vite build && mkdir -p api/static && cp -r dist/. api/static/",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/index.ts" }
  ]
}
