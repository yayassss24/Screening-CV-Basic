{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "vite build && cp -r dist/. public/",
  "routes": [
    { "src": "/api/(.*)", "dest": "api/index.ts" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
